# Personal Voice Assistant | Streamlit + Groq API

# 1. pip install -r requirements.txt

# 2. streamlit run main.py

# 3. Generate the API key from the Groq cloud
# 4. $env:GROQ_API_KEY="groq api key should be pasted here"
# API KEY should not be shared to any one -- 

#